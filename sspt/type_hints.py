
from typing import *

# Custom types to make type hints clearer:
Oid = int          # Type for objectId
Var = str          # Type for variable names (SSP symbols)
Timestamp = float  # Type for timestamps (as returned from time.time())
RegIx = int        # Registry entry (Index into Registry)
